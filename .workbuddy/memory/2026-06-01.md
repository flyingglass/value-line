# 2026-06-01 工作日志

## 腾讯控股 (00700) 报告生成
- CF=10.0x, 15年数据 (2011-2025), 264个月K线
- 8步流水线全部通过, 72项校验 0 mismatch

## 中国民航信息网络 (00696) 报告生成
- CF=10x, 全流程: fetcher → PDF(13份) → revenue_structure → engine → generate_report
- 年报PDF港交所披露易自动下载, 2019-2025
- MDA提取仅245char(质量=0), 引擎fallback用config.py已有commentary
- 营收结构从2025年报P12-13手动提取 (5业务分部)
- 验证: 69/72 PASS, 3 FAIL (DEPRECIATION 8/15y, LT_DEBT 7/15y, 1 cross-check)
- 报告: report/TravelSky_Technology.html (114KB)
- 报告: report/Tencent_Holdings.html (119KB)

## engine.py — 固化的关键修复

### 1. indicators 回退计算 (核心)
**问题**: AKShare港股 indicators 仅9年 (2017-2025), engine 直接 skip 导致 data/cf_line 只有9年
**修复**: build_metric_table() 双路径:
- 标准路径: indicators 表取值 (不变)
- 回退路径: income/balance/cashflow 原始表当面计算 24 项指标
**影响**: data 9年→15年, cf_line 9点→15点, 缺口年份全覆盖

### 2. 税率回退 (item_code)
**问题**: income.item_name 编码乱码导致 `税项`/`除税前利润` 搜索失败
**修复**: 改用 financial_item_by_code 配合 STD_ITEM_CODE (004012001=税项, 004011999=除税前利润)

### 3. DIV_YIELD 零值处理
**问题**: DPS=0 年份 DIV_YIELD=None → 验证失败 (8/15y)
**修复**: DPS=0 时设 DIV_YIELD=0.0 (不分红→股息率=0%), 语义正确

### 4. build.py 编码
- Unicode ✓/✗ → ASCII [PASS]/[FAIL]

### 5. shares 回退
- share_count(rd) 为空时 → config.STOCKS.shares 兜底

## generate_report.py — 15年布局适配
- showYears: Y.slice(-10) → Y.slice(-15)
- 页面宽度: 1280 → 1360px
- 左栏: 275 → 245px
- 统计表: font 8.5→8px, padding 8→4px, 第一列 110→130px
- K线高度: 260→240px
- K线年限: kl.filter(>=showYears[0]) 与指标对齐
- LEGENDS列: colgroup 130px (table-layout:fixed 覆盖 td width → 改用 colgroup)
- Year列: 40px
- Quarterly标题: white-space:nowrap; font 9.5px (左栏245px下防换行)
- 修复重复 padding bug (inline + tdStyle)

## 文档更新
- docs/BUILD.md: 新增回退计算章节
- docs/USER_GUIDE.md: 新增年份覆盖说明
- docs/DATA_VERIFICATION.md: indicators 表标注早年回退
- docs/VL_REGION_ALIGNMENT.md: 完整布局/样式/年份覆盖规范更新

## mda_text 对接修复 → 数据自生成 (终版)
**问题**: extract_mda.py 从PDF提取 mda_text → SQLite → report_data.json, 但 generate_report.py 从未使用;
          腾讯等复杂年报提取质量差, 且 config.py 没人会手动维护
**修复**:
- extract_mda.py: 新增 _is_narrative()过滤财务数据句, scoring分类替代布尔匹配, mda_quality质量门
- engine.py: 新增 _parse_mda_text()解析分段文本 + _build_business_from_data()/_build_commentary_from_data()数据自生成
- engine.py build_report(): quality=1用PDF, quality=0自动从财务数据生成 (零config依赖)
- generate_report.py: BUSINESS读analyst.business, Commentary动态渲染 (区分PDF/自生成模式)
- build.py Step 0: business_desc/commentary 降为INFO
