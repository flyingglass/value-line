# 2026-06-02

## 分众传媒 (002027) 报告生成

- CF=10.0x, 8步流水线全通过 (71/71 PASS)
- 报告: `report/Focus_Media.html` (116KB)

### Step 4: 营收拆分 (AI自动提取)
- 从2021-2024年报PDF自动提取营收结构
- by_product: 楼宇媒体/影院媒体/其他 (3 dims)
- by_industry: 日用消费品/互联网/交通/通讯/娱乐休闲/房产家居/商业服务/其他 (8 dims)
- 44条记录, pct全部100%自洽

### engine.py 修复 (A股适配)
1. **cn_map 扩展**: 新增 004027003→basic_eps, 004012001→income_tax_expense, 004011999→profit_total
2. **TAX_EBT A股回退**: indicators路径无TAX_EBT时回退到income表当面计算
3. **revenue_structure 年份回退**: 最新年无数据时向前查找(最多5年)
4. **dim_type 扩展**: 新增 by_product / by_industry

### build.py 修复
- EPS HKD检查: A股跳过(仅港股需要)
- CrossCheck验证: 2018+年份0 mismatch即通过; 早年借壳前数据mismatch仅警告

### 已知: 早年验证mismatch
- 2013-2017 共15项mismatch(BPS/SHARES/Depreciation)
- 根因: 分众传媒2015年借壳七喜控股(002027)上市, 早年数据为不同主体
- 2018+ 年份零mismatch, 数据可靠

## 第二轮重建 (DB已删除重拉)

### 大量 AKShare THS API 格式变更修复
1. **fetcher.py**: `_parse_cn_amount()` 解析中文单位(亿/万)
2. **fetcher.py**: `fetch_spot_cn` 容错(API不可用时跳过)
3. **fetcher.py**: indicators `stock_financial_abstract_new_ths` 从宽表→长表格式适配
4. **fetcher.py**: 分红 `stock_dividend_cninfo` 列名变更适配(派息比例→DPS, 报告时间→年份)
5. **fetcher.py**: cashflow `stock_financial_cash_ths` 容错(API间歇不可用)
6. **engine.py**: `_THS_NAME_MAP` 旧中文名→新THS列名映射 (总资产→*资产合计 等30+项)
7. **engine.py**: `financial_item()` 新增三层回退: 直接→THS新名→英文名
8. **engine.py**: `cn_map` 从英文名改为新THS中文名
9. **build.py**: 宽松字段验证 (DEPRECIATION/LT_DEBT 阈值降为1)
10. **extract_mda.py**: PDF提取不足时保留原文(不用等report_data.json)

### 已知限制
- 现金表 API `stock_financial_cash_ths` 暂不可用 → DEPRECIATION 数据缺失
- 行情 API `stock_zh_a_spot` 暂不可用 → spot 手动填入(5.41元)
