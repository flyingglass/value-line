# 2026-05-31 工作日志

## 汇率数据入库
- 通过 AKShare `currency_boc_safe` 获取国家外汇管理局官方中间价数据
- 存入 `data/fx_rates.db`，表 `daily_rates`
- 包含 usd_cny、hkd_cny 两个字段，单位 100外币兑CNY
- 覆盖 1994-01-01 至 2026-05-29，7973条，每日粒度

## Quarterly Data Q1/Q4 垂直分割线对齐修复
- **问题**: Q1 在 14%宽度单元中右对齐，padding-left 无效，距左分割线太远；Q4 右对齐 padding-right=3px 靠右分割线 — 左右不对称
- **修复**: Q1 表头+数据行改为 `text-align:left`，`padding-left:3px` 生效，距左分割线 3px = Q4 距右分割线 3px
- **回退**: 之前 `padding:0 3px` 统一化改动被拒，回退为原 `padding-right:3px` + border侧 `padding-left:3px` 模式
- **文档**: 更新 VL_REGION_ALIGNMENT.md 对齐规则表
- **提交**: commit 798a827
